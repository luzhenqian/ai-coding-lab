## 1. 数据模型扩展

- [x] 1.1 在 Prisma Comment 模型中添加 `parentId` 可选自引用字段和 `updatedAt` 字段
- [x] 1.2 运行 `prisma migrate dev` 生成并应用数据库迁移
- [x] 1.3 更新 Zod 评论验证 schema，增加 `parentId` 可选字段

## 2. API 路由扩展

- [x] 2.1 在 `/api/comments/route.ts` 添加 GET 方法，支持按 articleId 查询、cursor 分页（每页 20 条）、包含嵌套回复
- [x] 2.2 在 `/api/comments/[id]/route.ts` 添加 PUT 方法，支持评论作者编辑自己的评论
- [x] 2.3 更新 DELETE 方法，级联删除子回复（有 parentId 引用的评论）
- [x] 2.4 更新 POST 方法，支持 parentId 参数，验证 parentId 指向顶级评论

## 3. 前端评论组件

- [x] 3.1 创建 `CommentForm` 客户端组件（评论输入框 + 提交按钮，未登录时显示登录引导）
- [x] 3.2 创建 `CommentItem` 客户端组件（显示评论内容、作者头像/名称、时间、编辑/删除/回复按钮）
- [x] 3.3 创建 `CommentList` 客户端组件（评论列表容器，支持"加载更多"分页）
- [x] 3.4 将文章详情页的静态评论展示替换为新的 CommentList 组件

## 4. 评论交互功能

- [x] 4.1 实现发表评论功能（调用 POST API，乐观更新列表）
- [x] 4.2 实现编辑评论功能（内联编辑模式，调用 PUT API）
- [x] 4.3 实现删除评论功能（确认弹窗，调用 DELETE API）
- [x] 4.4 实现回复评论功能（点击回复按钮展开回复表单，提交时带 parentId）

## 5. 管理后台评论管理

- [x] 5.1 创建 `/admin/comments/page.tsx` 评论管理页面，显示所有评论分页列表
- [x] 5.2 添加按文章筛选功能
- [x] 5.3 实现管理员删除评论功能
- [x] 5.4 在管理后台侧边栏添加"评论管理"导航链接

## 6. 集成与验证

- [x] 6.1 验证未登录用户可查看评论但无法发表
- [x] 6.2 验证评论作者可编辑/删除自己的评论，不可操作他人评论
- [x] 6.3 验证管理员和文章作者可删除任何评论
- [x] 6.4 验证嵌套回复正确显示和删除级联
